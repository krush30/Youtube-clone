import React from 'react'

const Button = ({ name }) => {

    return (
        <div className=''>
            <button className='px-5 m-5 bg-gray-300 rounded-lg py-2 '>{name}</button>

        </div>
    )
}

export default Button;
